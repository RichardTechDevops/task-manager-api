# ClusterIP：集群内部通过 8080 访问 Pod。
apiVersion: v1
kind: Service
metadata:
  name: task-manager-api
  namespace: task-manager
spec:
  type: ClusterIP
  selector:
    app: task-manager-api
  ports:
    - name: http
      port: 8080
      targetPort: http
