services:
  task-manager-api:
    build: .
    image: task-manager-api:local
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      LOG_LEVEL: INFO
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health')"]
      interval: 15s
      timeout: 5s
      retries: 3
